arborist_endpoint: 'NONE'
global_reader_role: 'dummyGlobalReaderRole'
pseudonyms:
  pseudonym_field: 'person.person_source_value'
  pseudonym_external_name: 'my_pseudonym_id'
# see tests/setup_cdm/README.md
atlas_db:
  host: localhost
  port: '5433'
  username: postgres
  password: mysecretpassword # pragma: allowlist secret
  db: postgres
  schema: atlas
# optional validation config:
validate:
  single_observation_for_concept_ids:
    # HARE concept id:
    - '2000007027'
worker_pool_size: 2
batch_size: 4
